import logo from "../../images/imag/logo.PNG";
import { Div } from "./homePage.styled";

const HomePage = () => {
  return (
    <Div>
      <img src={logo} alt={logo} />
    </Div>
  );
};

export default HomePage;
